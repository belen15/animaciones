# React motion

    npm i framer-motion
    
    import { motion } from "framer-motion";
    
     return(
     <motion.div
        className="caja"
        variants={desplazar}
        animate="fin" 
      />
     )
